# ===========================================
# Agents Of 272 DRL Driving Project Requirements
# ===========================================
#
# Installation:
#   pip install -r requirements.txt
#
# Notes:
#   - PyTorch installation may need to match your CUDA version
#   - highway-env requires pygame for rendering
#   - stable-baselines3 here is the Gymnasium-compatible version
#   - imageio is used for saving GIFs from visualize scripts
#
# ===========================================

# Core RL and ML
stable-baselines3>=2.0.0          # PPO, VecEnv, callbacks, etc.
torch>=2.0.0                      # PyTorch backend for SB3 policies
torchvision>=0.15.0               # For CNN policies (grayscale observation)

# Environment and Simulation
highway-env>=1.8.0                # Highway / merge / intersection driving envs :contentReference[oaicite:0]{index=0}
gymnasium>=0.29.0                 # Gymnasium API used by SB3 and highway-env :contentReference[oaicite:1]{index=1}
pygame>=2.5.0                     # Rendering backend required by highway-env

# Experiment Tracking and Visualization
wandb>=0.16.0                     # Weights & Biases logging (single + multi-scenario helpers) 
matplotlib>=3.8.0                 # For any plots you make locally
imageio>=2.34.0                   # Saving GIFs in visualize.py / visualize_multi_scenario.py 

# Scientific Computing and Utilities
numpy>=1.26.0                     # Numerical arrays (also pulled in by torch/SB3)
tqdm>=4.66.0                      # Progress bars (used by SB3 internally)
psutil>=5.9.0                     # Optional system monitoring (useful but not required)

# Optional: development / notebooks / extra ML tools
# pandas>=2.0.0                   # Data analysis / result processing
# scipy>=1.12.0                   # Extra scientific utilities
# tensorboard>=2.15.0             # Alternative to W&B for logging
# jupyter>=1.0.0                  # Notebooks for exploration
# pytest>=8.0.0                   # Testing framework
# scikit-learn>=1.3.0             # Extra ML utilities
